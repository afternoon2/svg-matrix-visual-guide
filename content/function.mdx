Drag the cursor inside the circle area to update each modifier's value.

You can also double click it to update the value using an input.

`X` modifying values are hightlighted with pink-ish color. `Y` modifiers are marked with dark orange.